// Temporarily completely disabled to fix React loading issues
// This hook was causing "Cannot read properties of null (reading 'useEffect')" errors

const usePageVisitTracker = () => {
  // All functionality temporarily disabled
  console.log('Page visit tracker called but disabled for debugging');
};

export default usePageVisitTracker;